# Frontend → Backend 契约消费回执模板

> 使用说明：Frontend 在 Gate 0/1 条件满足后填写本模板。正式消费前必须完成来源和哈希核验；契约疑问写入 `proposals/frontend/`，不得自行修改或推断权威 API。

## 1. Frontend 来源信息

- 前端项目：`G:/Dev/backend/Club/CoderClubFront`
- 来源分支：
- 来源提交哈希（完整）：
- 工作区路径：
- 实际消费契约路径：
- 实际消费契约 SHA-256：
- 期望契约 SHA-256：
- OpenAPI 版本：
- 路径数量：
- 操作数量：

## 2. 契约校验

- Backend 回执路径：
- Backend 源提交哈希：
- Backend 源 SHA-256：
- 交接转换说明：
- `npm run api:check` 命令：
- `npm run api:check` 结果：
- 其他直接契约测试命令：
- 其他直接契约测试结果：

哈希不一致时，Frontend 必须停止正式 API 消费，并将差异、复现命令和实际文件来源写入 `proposals/frontend/` 或本回执的阻塞项，不得通过修改期望值掩盖差异。

## 3. 首轮消费范围

| 模块 | 正式方法/路径 | 鉴权结论 | 401/403 处理 | 验证状态 |
| --- | --- | --- | --- | --- |
| 认证与用户信息 |  |  |  | 未开始 |
| 分类与标签 |  |  |  | 未开始 |
| 题目分页与详情 |  |  |  | 未开始 |
| OSS 上传 |  |  |  | 未开始 |

Frontend 只使用 PM 已确认的正式方法；分类、标签更新和删除默认使用 PUT/DELETE，POST 兼容端点须有明确授权才可调用。

## 4. 环境与验证

- Node 版本：
- 包管理器版本：
- `npm run api:check`：
- `npm test`：
- 其他验证命令：
- 实际服务地址：
- 验证时间：
- 阻塞错误：
- 已知限制：

## 5. 问题与后端动作

- 契约问题编号：
- 复现请求与响应：
- 影响页面或状态：
- 建议方案：
- 是否需要 Backend 提交：是 / 否
- 是否需要 PM 决策：是 / 否
- Backend 接收动作：

## 6. Frontend 声明

我确认以上消费文件、来源、哈希和验证结果真实可复核；未确认的字段、方法、鉴权或错误码没有被前端自行推断。

- Frontend 角色：
- 日期：
