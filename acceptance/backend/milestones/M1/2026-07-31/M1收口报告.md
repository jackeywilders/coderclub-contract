# M1 收口报告

> **日期**：2026-07-31
> **依据**：《2026-07-31-M1深度审查报告》v1.0（裁决 Approved，2 个 🔴 Bug 需联调前修复）
> **性质**：M1 里程碑正式收口

## 一、深度审查发现项处置

| 发现项 | 处置 | 证据 |
|--------|------|------|
| A1 🔴 `SubjectInfoMapper.xml` 缺 `categoryId`/`labelId` null 守卫 | ✅ 已修复：补 `<if test>`，与 `subjectDifficult`/`subjectType` 一致 | 见 commit `b2f9b2c`；运行时确认排入 M3 联调 |
| A2 🔴 `countByCondition` null 守卫不一致 | ⚠️ **复核为误报**：MyBatis-Flex 1.11.6 `eq(column, null)` 自动跳过条件，实测 `toSQL()` 不产生 `category_id = NULL`。已补显式三参守卫（no-op，风格统一）+ 2 个单元测试锁定行为 | `SubjectInfoServiceImplTest`（2/2 PASS），见 commit `f5d044b` |

## 二、全量回归（实测）

| 项 | 结果 |
|----|------|
| mvn install -DskipTests | BUILD SUCCESS |
| 单测（common 4 + auth-domain 7 + subject-infra 2） | Tests run: 13, Failures: 0, Errors: 0 |
| `.error(` / `getResult` / 枚举死值 残留 | 0 |

## 三、M2 交接清单（来自深度审查，8 项）

已同步进 `docs/backend/roadmap/2026-07-31-后端开发计划.md` §2.5，此处不重复。

## 四、M1 最终裁决

**✅ M1 收口完成。** 变更集迁移完整（深度审查 B/C 角度零残留），A1 已修复，A2 经实证复核为误报并加回归保护，8 项优化全部移交 M2。Subject 模块可进入 M3 联调。
