# 项目级治理文件补强建议清单

> 日期：2026-08-18
> 用途：对照全局规范 v2.1（`G:\Dev\agent\agens rules\AGENT.md`），对 CoderClub 三仓库（交接仓库 / 后端 CoderClub / 前端 CoderClubFront）的 AGENTS.md 与 CLAUDE.md 做合规检查后，产出"由全局兜底 ⇒ 是否应显式化为项目级约定"的建议清单。
> 结论基线：三仓库配置均为最新版（2026-08-18 已 fetch 核实），无与全局规范冲突的条款；差异全部落在"项目已覆盖（更强）"或"项目未写、全局兜底"两类。
> 评估标准：① 协作风险（缺失是否造成跨角色/跨仓库纠纷）> ② 变更频率（项目日常是否高频踩坑）> ③ 全局兜底充分性（全局 v2.1 是否已足够，足够则不补）。
> 状态：**已实施**（A/B/C/H 补强项已于 2026-08-18 后依次落地；D/E/F/G 维持"不补"留档）。2026-09-01 治理批次复核：代码仓库侧均已含对应条款（后端/前端 `AGENTS.md` 远程优先核验、`CLAUDE.md` 验证拦截+运行资源自管理、前端 `AGENTS.md` 敏感凭据约束），本清单为历史决议留档，不再有待办。

## 一、评级汇总

| 组 | 条目 | 评级 | 落点 | 状态 |
| --- | --- | --- | --- | --- |
| A | 远程优先核验 | **补** | 后端/前端 `AGENTS.md` | 待授权 |
| B | 验证被沙箱拦截时处理 | **补** | 后端/前端 `CLAUDE.md` | 待授权 |
| C | 运行资源自管理 | **补** | 后端/前端 `CLAUDE.md` | 待授权 |
| D | 审批模式认知 | 不补 | 留档 | — |
| E | 磁盘级操作安全预览 | 不补 | 留档 | — |
| F | pwsh7 版本约束 / 终端分工 | 不补 | 留档 | — |
| G | 路径/编码/批量操作预览 | 不补 | 留档 | — |
| H | 前端敏感约束扩围 | **补** | 前端 `AGENTS.md` | 待授权 |

## 二、逐条评估

### A 组 · 远程优先核验（后端/前端未写，交接仓库规则 9 已补强）

- 评估：①协作风险**高**（"已合入/已签署/已完成"声称的核验是跨角色纠纷源头；交接仓库规则 9 是治本，代码仓库侧也应引用）②变更频率中 ③全局兜底充分（v2.1 第 16 条）。
- **推荐补（代码仓库）**。落点：后端 `AGENTS.md`、前端 `AGENTS.md` 各新增一条，引用交接仓库规则 9，明确"复验时先 `git fetch origin` 再按 R1（远端分支/PR 可见）/ R2（已合入目标分支）判定，不依据本地旧状态或未 fetch 的 worktree"。

### B 组 · 验证被沙箱/环境拦截处理（三仓库均未写）

- 评估：①协作风险**高**（"跳过验证却声称完成"是验收与签署信任的地基，一旦发生难以追溯）②变更频率中 ③全局兜底充分（v2.1 代码开发节第 5 条【禁止】明确）。
- **推荐补（执行角色文件）**。落点：后端 `CLAUDE.md`、前端 `CLAUDE.md` 各在验证相关小节补一句："验证命令被沙箱/环境拦截时如实报告并申请更高权限；【禁止】跳过验证却声称'已完成/已通过'"。交接仓库不强补（证据门禁已隐含）。

### C 组 · 临时产物与运行资源自管理（三仓库均未写）

- 评估：①协作风险中 ②变更频率**高**（开发/调试/CI 场景高频产生临时文件与后台进程、端口占用）③全局兜底充分。
- **推荐补（执行角色文件）**。落点：后端 `CLAUDE.md`、前端 `CLAUDE.md` 各补一句："任务产生的临时文件、后台进程、启动的服务、占用的端口，任务结束后清理或明确说明，不遗留僵死进程与占用"。交接仓库不强补（治理文档不产运行资源）。

### D 组 · 审批模式认知（三仓库均未写）

- ①协作风险低 ②变更频率低 ③全局兜底充分（v2.1 第 3 条【建议】）。**不补**，留档。

### E 组 · 磁盘级操作安全预览（三仓库均未写）

- ①协作风险低 ②变更频率低 ③全局兜底充分（v2.1 第 10 条）。**不补**，留档。

### F 组 · pwsh7 版本约束 / 终端分工（三仓库均未写明）

- **2026-08-18 实锤澄清**：后端开发内容中 pwsh 并非全程必须。已核实：构建（`mvn`）、测试、git、文件、日志处理全部跨平台，git bash 完全胜任且对 agent 更友好（纯文本稳定输出）；唯一必须 pwsh 的场景是**运行项目自带的 `.ps1` 启动脚本**（`start-auth.ps1`/`start-subject.ps1`/`start-oss.ps1`，内含 Nacos 环境变量预配置）与 Windows 系统级操作；CI 本身跑在 `ubuntu-latest`（Linux bash）。
- ①协作风险中 ②变更频率中 ③全局兜底充分（v2.1 第 4 条【必须】管住"用 PowerShell 时须 pwsh 7"；第 5 条【建议】管住"代码开发默认 git bash / 跑 .ps1 与系统管理用 pwsh"）。
- **不补**，留档。可选增强（已弃）：在 CLAUDE 加"启动 .ps1 用 pwsh、其余开发用 git bash"分工提示——因全局分工语义已覆盖，评估后不新增条目。

### G 组 · 路径/编码/批量操作预览（三仓库均未写）

- ①协作风险中 ②变更频率低 ③全局兜底充分。**不补**，留档（与 `.editorconfig`、构建结构无交互的空泛基础规则）。

### H 组 · 前端敏感约束范围过窄（仅 ApiFox 密码）

- ①协作风险中 ②变更频率低 ③全局兜底充分；但前端日常处理 token/用户数据，仅覆盖 ApiFox 有"灯下黑"风险。
- **推荐补（前端 AGENTS.md）**。落点：前端 `AGENTS.md` API 文档小节，将"API 密钥、token、账号凭据不落仓库"并入现有 ApiFox 约束，保持简短。

## 三、落地路径

1. **本清单**写入交接仓库 `docs/agents/`（协调 PM 可写，随本 PR 合入 main）。
2. 代码仓库补强项（A/B/C/H）按以下顺序等待用户逐条授权后改动，每次改动前提供具体 diff 预览：
   - 后端 `AGENTS.md`（A）
   - 后端 `CLAUDE.md`（B、C）
   - 前端 `AGENTS.md`（A、H）
   - 前端 `CLAUDE.md`（B、C）
3. D/E/F/G 不落地，留档；若未来项目场景变化（如引入高频磁盘清理、终端切换冲突），再按本清单重新评估。

## 四、版本记录

- 2026-08-18：创建（brainstorming + grilling 讨论定案；评估标准 ①②③；F 组经 pwsh 事实澄清实锤后维持不补）。